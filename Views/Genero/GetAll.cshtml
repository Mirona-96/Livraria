@model IEnumerable<Livraria.Models.Domain.Genero>
@{

}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" asp-action="Add">+ Adicionar Género</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4">
            <h3>Géneros</h3>


            <table class="table table-striped table-bordered table-dark">
                <thead>
                    <tr>    
                        <th>Géneros</th>
                        <th>Acção</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                            <td>@item.Nome</td>
                            <td>
                                <a class="btn btn-success" asp-controller="Genero" asp-action="Update" asp-route-id="@item.Id">
                                    <i class="bi bi-pen"></i>
                                </a> |
                                <a class="btn btn-danger" asp-controller="Genero" asp-action="Delete" asp-route-id="@item.Id"
                                   asp-route-returnUrl="@Url.Action("Index")"
                                   onclick="return confirm('Certeza que pretende eliminar registo?')">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>